/**
 * ESLint配置
	* 主要用于配置IDE，重新定义代码风格
	* */

//此项是用来告诉eslint找当前配置文件不能往父级查找	 
root = true

// 对所有文件有效，[*js]只对js文件有效
[*]
charset = utf-8
indent_style = space
indent_size = 2
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true
